# Known Limitations

- Mermaid is exported as Markdown text and not rendered graphically inside the page.
- ZIP export depends on browser Blob download support.
- API key entry is local-only; users should avoid shared browsers for sensitive keys.
- Desktop is the primary target; mobile is responsive but not deeply optimized.
- UI strings are primarily English, with layout designed to tolerate Chinese content.
